const accountId = 1234;

let accountName = "Suraj";

var accountPassword = "suraj123";

/*
    Prefer not to use var. 
    Beacause it doesn't have block scope or functional scope.
*/

email = "suraj@gmail.com";   // Don't use this. // Just keep in mind that js can do this.

//accountId = 111;  // *Cannot assign value to a constant.

console.table([ accountId, accountName, accountPassword, email]);
